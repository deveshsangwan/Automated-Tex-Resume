% fonts.cls is now empty or contains only minimal class definition to avoid conflicts.
\ProvidesClass{fonts}[2014/04/30 CV class]
\NeedsTeXFormat{LaTeX2e}
% (Intentionally left blank to avoid conflicts with main .tex preamble)

